firstPass(){
    // store names of all global variables
    // if already present throw error
}

// 0 - Point<Record> Fd_list
// 1 - Complex<Record> Fd_List
// 2 -
// 3 - n
typeDefPass(){
    // start with first typedef
    // for each entry in typedefSymTab
        // if type not computed
        // computeType()
}

computeType(typedef){
    //for each field - if primitive, add to typeExpression of tdef
    //else computeType(field) and add to tdef's typeExpression
}